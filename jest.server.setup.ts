// Server-specific test setup
// Add any server-side mocks or global setup here

// Mock fetch for server components
global.fetch = jest.fn();

// Add any other server-side globals your tests might need
